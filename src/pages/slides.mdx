---
navigationLabel: Tutorial
---
<Box p={2} mb={2} color="white" bg="surface">
    <Link href="/">Home</Link>
</Box>
<br/>

# Tutorial

This is a walkthrough on how to build an ecommerce site using a few known platforms and tools.
The goal is to keep it simple as possible and be has detailed as possible in the steps description.
The steps will be updated as we go and hopefully we will not take too long :)

### If you need help or just want to drop in some feedback reach us out on Twitter or submit an issue in the repository

<SourceList>
  {source => (
    <div>
      {source
      .sort((a, b) => new Date(b.node.frontmatter.date) - new Date(a.node.frontmatter.date))
      .reverse()
      .map((edge, index) => {
        const {
          frontmatter: { title, path },
          fields: { slug },
        } = edge.node
        return (
          <Message key={index} variant="success" mt={40}>
            {title}&nbsp;<Link href={slug}>here</Link>
          </Message>
        )
      })}
    </div>
  )}
</SourceList>
