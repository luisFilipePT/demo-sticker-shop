import React from "react"
import { ToastProvider } from "react-toast-notifications"

/* TODO: get the data */
const ProductTemplate = () => {
  return (
    <ToastProvider>
      {/* TODO: call the product component */}
    </ToastProvider>
  )
}

// TODO: add query for single product

export default ProductTemplate
